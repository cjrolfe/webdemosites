# Swordthain Infra (CDK)

Shared AWS infrastructure for swordthain.com, in TypeScript via AWS CDK. Deployed independently of `apps/media-app` and `apps/playground` — those apps' own CDK stacks (added in later phases) will reference this stack's outputs.

## Stacks

### `SwordthainCiStack`
GitHub Actions OIDC trust (`token.actions.githubusercontent.com`) plus five purpose-scoped roles — no long-lived AWS keys stored in GitHub. All five roles' trust policy is restricted to `repo:cjrolfe/swordthain:ref:refs/heads/main`, so only a genuine push to `main` (never a PR, never a fork) can assume them.

- **`swordthain-playground-ci`** — scoped to exactly the playground's existing manual deploy: S3 read/write on `swordthain-demo-sites`, `lambda:UpdateFunctionCode` on `swordthain-automation`, `cloudfront:CreateInvalidation` on `SwordthainPlaygroundStack`'s `LabsDistribution` (a real construct reference — both stacks are us-east-1). Originally scoped to the pre-cutover playground distribution (`E1AUXZ6C0Z7J9P`); repointed once that distribution was decommissioned post-cutover (`LabsDistribution` had been playground's real distribution since the labs.swordthain.com work, this permission just hadn't caught up) — `.github/workflows/deploy-playground.yml`'s invalidation call needed the matching update, or the next playground deploy would've invalidated a distribution that no longer exists.
- **`swordthain-media-app-ci`** — S3 read/write on `SwordthainMediaAppDataStack`'s `SiteBucket` (eu-west-1), `cloudfront:CreateInvalidation` on `SwordthainMediaAppHostingStack`'s `SiteDistribution` (us-east-1). The bucket is passed into `CiStack` as a plain name string (`mediaAppSiteBucketName`), not a construct reference — since the two stacks are in different regions, a real CDK cross-stack reference would need `crossRegionReferences`, which this codebase avoids (see "Region split" below). The distribution ID *is* a real construct reference (`mediaAppHostingStack.siteDistribution.distributionId`) since `CiStack` and `MediaAppHostingStack` are both us-east-1 — no region boundary to cross there.
- **`swordthain-infra-ci`** — only `sts:AssumeRole`/`sts:TagSession` on the CDK bootstrap roles, **in both `us-east-1` and `eu-west-1`** (`bootstrapRegions` prop) — one set of bootstrap roles per region, both needed now that stacks deploy to two regions. Those bootstrap roles already carry the permissions CDK needs (see "Prerequisites" above) — this role is a narrow bridge into them, not a second copy of `AdministratorAccess`.
- **`swordthain-regression-test-ci`** — `ssm:GetParameter` on the one fixed-OTP parameter, plus `dynamodb:PutItem`/`DeleteItem` on `swordthain-media-items` (eu-west-1, referenced by literal ARN — cross-region, same pattern as the bucket name above) for `infra/regression-tests`' synthetic video-item fixture. No S3/Lambda/CloudFront access at all — everything else the suite does is a plain HTTPS call to Cognito and the app's own API Gateway, same as any real client. The DynamoDB grant was added after the *first* real CI run correctly caught its absence (worked locally only because ambient AWS CLI credentials had broader access, masking the gap) — see `infra/regression-tests/README.md`.
- **`swordthain-a11y-test-ci`** — the same `ssm:GetParameter`/`dynamodb:PutItem`/`DeleteItem` grants as `swordthain-regression-test-ci` above (`infra/a11y-tests` signs in as the same fixed-OTP account and needs its own disposable media fixture for `tests/modal.spec.ts`), kept as a *separate* role rather than reused so the functional and accessibility suites' CI permissions stay independently auditable even though they're currently identical.

See `.github/workflows/` at the repo root: `deploy-playground.yml` triggers on push to `main` scoped to `apps/playground/**`, independent of the other two apps. `deploy.yml` covers both `infra/**` and `apps/media-app/**` in one workflow (merged 24 Aug 2026 — see below for why) with two conditional jobs, `deploy-infra`/`deploy-media-app`, each gated on whether its own path actually changed in the push (a `changes` job computes this via a plain `git diff` against the push's before/after SHA, no third-party action) — so a push touching only one area still only runs that area's job, same as before the merge. `deploy-media-app`'s job builds the SPA (`npm run build`, which also type-checks via `tsc -b`) then syncs `dist/` to `SiteBucket` and invalidates `SiteDistribution` — no CDK involved, matching how `deploy-playground.yml` handles its own static assets. `regression-test.yml` and `a11y-test.yml` both run after `deploy.yml` succeeds (`workflow_run` trigger, not path-based), running `infra/regression-tests`' and `infra/a11y-tests`' suites against whatever's now live. `validate-infra.yml`, `validate-playground.yml`, and `validate-media-app.yml` run on PRs with no AWS credentials at all (type-check + `cdk synth` / build, Python compile-check).

**Why `deploy-infra.yml` and `deploy-media-app.yml` got merged into one `deploy.yml`**: as two fully independent workflows, a single push touching both `infra/**` and `apps/media-app/**` fired two separate deploy runs completing on their own schedules. `regression-test.yml`/`a11y-test.yml` listened for *either* to finish and sometimes ran as soon as the first of the two completed — testing a half-deployed system and failing for reasons unrelated to the actual change (observed live multiple times, always self-resolving on the second, post-both-deploys run, but wasting CI time and creating a confusing false-negative signal each time). Merging into one workflow means exactly one `workflow_run` completion event fires downstream, only once every job that needed to run actually has — and as a side effect, if one deploy job fails while the other succeeds, the whole workflow correctly reports failure instead of tests firing off the succeeding half alone.

### `SwordthainMediaAppDataStack`
`apps/media-app`'s data plane — **deployed in `eu-west-1`**, not `us-east-1` like every other stack (see "Region split" below for why, and for `SwordthainMediaAppHostingStack`, the small `us-east-1` stack that fronts this one's `SiteBucket` with CloudFront).

- **`MediaBucket`** (`swordthain-media-<account-id>`) — fully private (`BLOCK_ALL` public access, `BucketOwnerEnforced`), SSE-S3 encrypted, versioned, TLS-enforced via bucket policy.
- **Intelligent-Tiering from day one**: a lifecycle rule transitions every object to the `INTELLIGENT_TIERING` storage class immediately (day 0). This only activates the built-in Frequent/Infrequent/Archive-Instant-Access tiers, which have zero retrieval delay — the optional Archive Access / Deep Archive Access tiers are deliberately *not* opted into (that needs a separate bucket-level Intelligent-Tiering configuration), since both carry a multi-hour restore delay the spec rules out for casual browsing.
- **Decision: no Glacier tiering, revisit only if the library grows much larger.** Evaluated moving cold folders into Glacier Flexible Retrieval or Deep Archive on top of Intelligent-Tiering (modeled at 2TB total, 80% idle 2+ months). At that scale the extra saving over what Intelligent-Tiering's automatic tiers already capture for free is real but small in absolute terms (roughly $15–18/month), while the multi-hour-to-two-day restore delay either Glacier tier requires directly conflicts with the casual-family-browsing use case — and it would need new, currently-unbuilt product surface (a restore-initiate endpoint, a "warming up" UI state, a way to notify the user when it's ready). Not worth building today. Revisit if the library grows into the tens of TB, where the savings would matter more relative to the added complexity; even then, opting into Intelligent-Tiering's own optional Archive Access tier (still automatic, no custom app code, same restore delay once activated) is a smaller lift than a bespoke per-folder Glacier flow.
- **CORS** allows `PUT`/`POST`/`GET` from `swordthain.com` / `www.swordthain.com` for presigned multipart uploads.
- Incomplete multipart uploads are aborted after 7 days.
- **`MediaItemsTable`** (`swordthain-media-items`) — `mediaId` PK, GSI `byFolder` (`folderId` + `uploadedAt`) for chronological per-folder listing. `RemovalPolicy.RETAIN`.
- **`POST /media/upload-url`** (`UploadUrlFn`, fronted by `MediaHttpApi`, Cognito JWT-authorized via `HttpUserPoolAuthorizer`) — takes `{folderId, fileName, contentType}`, returns a presigned S3 PUT URL good for 1 hour. Object key: `originals/{folderId}/{mediaId}/{fileName}`. Single-PUT only (S3's 5GB hard limit) — true resumable multipart upload for very large files is a fast-follow, not built yet. Supported content types: JPEG, PNG, HEIC/HEIF, MP4, MOV, M4V.
- **Thumbnail generation** (`ThumbnailFn`, S3-triggered on `originals/*` `ObjectCreated`) — writes `thumbnails/{folderId}/{mediaId}.jpg` and the finalized `MediaItems` record (this is what actually creates the DB row — `UploadUrlFn` itself touches no DB, so an unused presigned URL just never produces a record). JPEG/PNG go through Sharp; HEIC/HEIF stills and video poster frames (1s in) go through ffmpeg, since Sharp's prebuilt Lambda binary excludes HEIF decode (libheif licensing).
  - **`SharpLayer`** — built at `cdk synth`/`deploy` time via `npm install --os=linux --cpu=x64 --libc=glibc sharp@0.33.5` (no Docker required; all three flags are required together, confirmed by testing — `--os`/`--cpu` alone silently skip the platform-specific `@img/sharp-*` optional dependency).
  - **`FfmpegLayer`** — `@ffmpeg-installer/ffmpeg@1.1.0` installed the same way as Sharp (npm's cross-platform flags, real per-platform `optionalDependencies`, npm registry only — no external website). Originally fetched a static build directly from johnvansickle.com at build time; switched after that caused a real (if transient) CI failure when GitHub Actions' runners couldn't reach it. See the layer's placeholder README for the trade-off (an older, 2021-vintage ffmpeg — fine for this project's actual poster-frame/HEIC-still needs).
  - Both layers and the full upload→thumbnail pipeline (photo and video) were verified end-to-end against the real deployed stack, not just unit-tested.
- **`FoldersTable`** (`swordthain-folders`) — `folderId` PK, GSI `byParent` (`parentFolderId` + `createdAt`). Top-level folders use the sentinel `parentFolderId: "ROOT"` rather than omitting the field, since a DynamoDB GSI skips items missing its indexed attribute — this keeps root and nested folders queryable through the same GSI.
- **Folder browsing** (`FoldersFn`):
  - `POST /folders` — `{title, parentFolderId?, date?, guestUploadEnabled?}` → creates a folder (validates `parentFolderId` exists if given, otherwise defaults to root). Owner-only — friends never create albums.
  - `GET /folders?parentId=...` — lists direct children (defaults to root-level folders if `parentId` omitted). Owner sees everything; a Member sees only children they (or an ancestor) have been granted access to.
  - `GET /folders/{folderId}` / `GET /folders/{folderId}/media` — same Owner-sees-all / Member-needs-access rule, enforced via `resolveAccess` (see below).
  - `PATCH /folders/{folderId}` — Owner-only, updates any of `{title, date, guestUploadEnabled, coverThumbnail, parentFolderId}`. Setting `parentFolderId` moves the folder: rejects moving it into itself (400) or into one of its own descendants (409, walks the ancestor chain from the target parent the same way `resolveAccess` does). Moving a folder changes what its descendants' *inherited* access resolves to for Members relying on an ancestor's share rather than an explicit one.
  - `DELETE /folders/{folderId}` — Owner-only. Blocked with a 409 if the folder still has sub-folders or media (has to be emptied first) — no cascading delete. Cleans up any `FolderShares` entries pointing at it.
- `RemovalPolicy.RETAIN` — this holds real media/folder data, never destroyed by a stack teardown.

#### A real bug caught by testing against the deployed API, not just Lambda invokes
Every Owner-only endpoint (`POST /folders`, the shares/invites/permissions-matrix routes) silently 403'd for real requests until building the admin UI (`apps/media-app`) surfaced it. Cause: API Gateway's HTTP API JWT authorizer serializes `cognito:groups` as a bracket-wrapped **string** (`"[Owner]"`) in `event.requestContext.authorizer.jwt.claims`, not a real JSON array — despite `@types/aws-lambda` allowing `string[]` there. `lambda/media/authz.ts`'s `isOwner()` had originally been written and "verified" against a hand-crafted Lambda invoke event using a real array, which passed — but never exercised the actual API Gateway code path. Fixed now (strips the brackets, splits on comma); the lesson generalized into a note in the repo-root `CLAUDE.md` since it's the kind of assumption easy to re-introduce elsewhere.

#### Sharing model
- **`FolderSharesTable`** (`swordthain-folder-shares`) — `folderId` PK + `userId` SK (GSI `byUser`: `userId` + `folderId`). Each item: `{folderId, userId, email, permission: "view"|"download"|"upload", grantedAt}`. `userId` is always the Cognito `sub` — **not** `Username`, which is a common trap here: even with `UsernameAttributes: [email]` configured on the pool, Cognito auto-generates a UUID as the actual `Username` (confirmed empirically — email only works as an alias for sign-in and Admin-API lookups by name, never as the literal `Username` value). Every place in this codebase that needs a stable per-friend identifier uses `sub`, resolved from an owner-supplied email via `AdminGetUserCommand` when needed.
- **Cascading access** (`lambda/media/access.ts`, `resolveAccess`) — walks from the target folder up through `parentFolderId` ancestors to `ROOT`, returning the *closest* explicit share found. This means sharing a parent grants access to everything beneath it, but a more specific share on a descendant overrides it — and a friend can be granted just one specific sub-folder without ever seeing (or being able to list) its siblings, even when querying "children of the parent" directly. Shared by both `FoldersFn` (browsing) and `UploadUrlFn` (upload gating).
- **`POST /folders/{folderId}/shares`** (`SharesFn`, Owner-only) — `{action: "grant"|"revoke", email, permission?}`. Resolves `email` → `sub` via `AdminGetUser`.
- **`GET /admin/permissions-matrix`** (`SharesFn`, Owner-only) — `{folders, shares, friends}` — a full scan/list dump (folders + shares tables, `Member` group from Cognito) for building the admin UI's grid in a later phase.
- **`POST /admin/invites`** (`InvitesFn`, Owner-only) — `{email, folderId?, permission?, message?}`. Creates a Cognito user (`AdminCreateUser`, `MessageAction: SUPPRESS` — Cognito's own invite email is never used, since the app is passwordless-only), adds them to the `Member` group, optionally grants immediate folder access, and sends a custom SES invite email (fixed shell + optional personal message; the fully templated/variable-driven version from the spec's admin UI is a later phase). **Production SES access is enabled** (us-east-1) — real invites to arbitrary friend addresses deliver. Earlier sandbox-mode testing used SES's `success@simulator.amazonses.com` mailbox simulator, which works regardless of sandbox status.
- **Upload gating** (`UploadUrlFn`) now checks `resolveAccess` too: Owner can always upload (folder existence is still validated); a Member needs an explicit `permission: "upload"` share — `view`/`download` alone isn't enough. Guest-upload-per-album (the `guestUploadEnabled` folder flag) isn't wired to this yet — that's a distinct, separately-flagged Phase 8 feature.
- Verified end-to-end against the real deployed stack: invited a real Cognito user + sent a real SES email, granted/revoked shares, confirmed cascading (parent share ⇒ all children visible), confirmed override (revoke parent + share one child ⇒ only that child visible, sibling stays hidden even when listing the parent's children), confirmed upload denied without the `upload` permission and allowed after granting it, and confirmed the permissions matrix reflects it all correctly (including catching and fixing the `Username`-vs-`email` bug above before it shipped).

#### Streaming & download
- **Progressive, not adaptive HLS, for now** — a deliberate scope call. True adaptive-bitrate HLS needs the browser to fetch a manifest *and* many segment files; a single S3 presigned URL only signs one object, not a whole path. Serving private HLS properly needs CloudFront with signed cookies in front of the bucket — real additional infrastructure (distribution, Origin Access Control, a CloudFront key pair, a cookie-signing Lambda), not just an extension of what's here. Deferred until the app needs CloudFront anyway (the root-domain migration in the spec's hardening phase). Until then: presigned S3 GET URLs for both photos and videos, same mechanism thumbnails already use — browsers play MP4/M4V directly with seeking via HTTP Range requests, which covers the "progressive" half of the spec's "progressive or HLS" wording.
- **`GET /media/{mediaId}/view-url`** / **`GET /media/{mediaId}/download-url`** (`MediaAccessFn`) — look up the `MediaItem`, resolve the requester's access via `resolveAccess` (Owner bypasses), and return a 5-minute presigned GET URL to the *original* object (not the thumbnail — thumbnails are for the grid, this is the full-res/full-stream asset). `download-url` additionally sets `ResponseContentDisposition: attachment` so the browser saves rather than navigates. Every call writes an `ActivityLog` entry.
- **`DELETE /media/{mediaId}`** / **`PATCH /media/{mediaId}`** (`MediaAccessFn`, Owner-only) — delete removes both S3 objects (`s3Key` original + `thumbnailKey`) in one `DeleteObjectsCommand`, then the `MediaItems` row; no cleanup of `PlaylistItems`/`ActivityLog` rows that reference it, since both already tolerate a missing media item (`available: false` / falls back to the raw `mediaId`). Update accepts `{description}` (trimmed, ≤140 chars, empty string clears it) — shown instead of `fileName` on every viewing surface (grid caption, Lightbox, playlist list/player), but never in the Activity log, its CSV export, or a downloaded file's name.
- **Permission hierarchy** (`access.ts`, `hasPermission`) — the three share tiers aren't independent flags, they're a ladder: `upload` ⊇ `download` ⊇ `view`. A `view`-only friend can open the lightbox but gets a 403 attempting to download; `download` or `upload` permission allows both. (`upload-url`'s existing exact-match check on `"upload"` still happens to be correct under this ladder, since upload is the top tier — no change needed there.)
- **`ActivityLogTable`** (`swordthain-activity-log`) — `logId` PK, GSIs `byUser` and `byFolder` (both + `timestamp`). `MediaAccessFn` writes every view/download here, matching the spec's `GET /media/{id}/view-url` / `download-url` behavior exactly ("logs 'view'" / "logs 'download'"). The read/query side is `ActivityFn` — see below.
- Admin UI: clicking a thumbnail opens a lightbox (`<img>` for photos, `<video controls>` for videos, both backed by `view-url`); a separate Download action calls `download-url` and triggers a browser download. Upload UI exists in `FolderBrowser.tsx`, gated by the same `resolveAccess` upload-permission check described above.
- Verified end-to-end in a real browser against the real deployed stack: photo lightbox, video playback with native seek controls, download triggering and completing, activity log entries written for both actions, and the permission ladder (`view` share → 200 on view-url, 403 on download-url; `download`/`upload` → both succeed).

#### Activity dashboard
- **`GET /admin/activity?folderId=...&userId=...`** (`ActivityFn`, Owner-only) — at least one of `folderId`/`userId` required (400 otherwise), matching the two `ActivityLogTable` GSIs: `folderId` queries `byFolder` (all activity in an album, across every friend — optionally further filtered to one friend in-memory), `userId` alone queries `byUser` (everything one friend has done, across every album they've touched). Results are capped at 200, most recent first, and enriched server-side with the friend's email (Cognito `ListUsers` with a `sub =` filter — `userId` in `ActivityLog` is the sub, not an email, same reasoning as the sharing model), the media's `fileName`, and the folder's `title`, so the frontend never has to resolve raw IDs itself.
- Admin UI **Activity** tab: folder and friend dropdowns (populated from the same `permissionsMatrix()` call `Permissions`/`Friends` already use — its folder list is a full table scan, not just root-level, so nested folders show up too), a results table, and an **Export CSV** button that builds the CSV client-side from the already-fetched rows (no server-side export endpoint — nothing to justify one at this data volume).
- Verified end-to-end in a real browser: generated real view/download activity by using the lightbox/download actions, confirmed the folder-filtered view showed correct enriched rows, confirmed the friend-filtered (`byUser`) query path directly, confirmed CSV export runs with no console errors, and confirmed the 403 (non-Owner) and 400 (no filter given) cases.

#### Hardening (partial — see below for what's deferred)
- **`infra/lambda/media/http.ts`** — a shared `jsonResponse` helper, now used by all seven media Lambdas (previously each had its own copy). Adds baseline headers to every response: `X-Robots-Tag: noindex, nofollow`, `Strict-Transport-Security` (HSTS), `X-Content-Type-Options: nosniff`, and `Cache-Control: no-store` (responses carry presigned URLs and permission/activity data — never cache). These only appear on responses that reach Lambda code — a 401 from the JWT authorizer itself (rejected before any Lambda runs) won't carry them, but there's no indexable content on an auth-rejected response anyway.
- **Rate limiting**: API Gateway stage-level throttling (20 req/sec sustained, burst 40) on `MediaHttpApi`'s default stage, via the L1 `CfnStage.defaultRouteSettings` (no L2 construct exposes this for HTTP APIs). This is a global cap across the whole API, not per-IP.
- **WAF was attempted and reverted** — worth recording why. **AWS WAFv2 cannot be associated with API Gateway HTTP APIs (v2) at all**, only REST APIs (v1), ALB, AppSync, Cognito, App Runner, Amplify, or Verified Access. Confirmed against AWS's own `AssociateWebACL` API reference, whose `ResourceArn` format is explicitly `arn:aws:apigateway:region::/restapis/api-id/stages/stage-name` — note `/restapis/`, not `/apis/`. A first attempt using the natural-looking `/apis/{id}/stages/$default` ARN deployed the `CfnWebACL` successfully but failed on `CfnWebACLAssociation` with "The ARN isn't valid," and CloudFormation rolled back cleanly. Getting real WAF bot-control managed rule groups in front of this API means either migrating off HTTP API to REST API, or putting CloudFront in front of it — both bigger moves than this pass's "safe pieces only" scope. CloudFront is the more sensible path given the spec's own architecture already wants it there; bundle it with the deferred playground-migration/root-domain cutover rather than a narrower one-off REST API migration now.
- **Still deferred**: WAF bot-control via CloudFront (see below — needs either a REST API migration or CloudFront in front of `MediaHttpApi`), and `robots.txt` as an actual served file for `swordthain.com`.
- MFA: already satisfied by the existing Cognito config from Phase 1 (`Mfa.OPTIONAL` with TOTP) — OTP-per-login is itself a second factor beyond just holding an account; nothing new needed here.
- Verified: response headers confirmed on a real Lambda invoke and (implicitly, via standard `AWS_PROXY` passthrough behavior already exercised throughout every prior phase) over the real HTTP path; throttle settings confirmed live via `aws apigatewayv2 get-stage`.

### `SwordthainMediaAppHostingStack`
The SPA's static hosting — closes the gap flagged above ("nothing hosts static files for the media-app yet"). Split into its own stack, in `us-east-1`, because CloudFront/ACM/WAFv2-for-CloudFront can only be managed from there — see "Region split" below. `SiteBucket` itself (the actual bytes) lives in `SwordthainMediaAppDataStack`, `eu-west-1`; this stack only fronts it.

- **`SiteDistribution`** — a CloudFront distribution in front of `SiteBucket`, referenced cross-region by name (`s3.Bucket.fromBucketAttributes(..., { region: "eu-west-1" })` — same pattern `SwordthainPlaygroundStack` already uses for its own cross-region origin), `index.html` as the default root object. No SPA-routing/custom-error-page config: `apps/media-app` has no client-side router, just one HTML page.
- **The bucket policy grant is a one-time manual step, not CDK-managed** — same reasoning and same fix as `SwordthainPlaygroundStack`'s `LabsDistribution`: CDK's `addToResourcePolicy` no-ops on an imported bucket, so `SiteDistribution`'s OAC needs an explicit `s3:GetObject` grant merged into `SiteBucket`'s policy via `aws s3api get-bucket-policy`/`put-bucket-policy` after the distribution exists.
- **`SiteWebAcl`** — a CloudFront-scope WAFv2 Web ACL (`AWSManagedRulesCommonRuleSet`, `AWSManagedRulesKnownBadInputsRuleSet`, `AWSManagedRulesAmazonIpReputationList` — all free managed rule groups) attached directly to the distribution. This is the real WAF the API hardening section above couldn't get (HTTP API v2 can't take WAFv2 at all); a CloudFront distribution can.
- **Custom domain: live.** `swordthain.com`/`www.swordthain.com` are aliased here via `MediaAppHostingStackProps.siteDomainNames`, reusing the existing ACM cert that already covered both names (it was issued for playground's original distribution). Cutover sequence: removed the aliases from playground's old distribution first (CloudFront refuses to let two distributions claim the same alias) via `aws cloudfront update-distribution`, waited for that to fully propagate (`Status: Deployed`), then deployed this stack with the aliases set, then repointed the Route 53 A records (`swordthain.com`/`www.swordthain.com`) to this distribution's domain. `labs.swordthain.com` was untouched throughout — it's a separate distribution/alias, unaffected by any of this.
- **`MediaHttpApi`'s CORS `allowOrigins`** (in `MediaAppDataStack`) includes `SiteDistribution`'s `*.cloudfront.net` domain as a literal string alongside `swordthain.com`/`www.swordthain.com`/`localhost:5173` — kept even after the cutover, since it's still useful for testing a new build against the distribution directly before it's live.
- No CDK `BucketDeployment` — matching the pattern already established for playground, a build artifact sync belongs in CI (`apps/media-app`'s own deploy workflow), not baked into the infra stack.
- Verified in a real browser, twice: first against `SiteDistributionDomainName` pre-cutover (folder create/browse, upload→thumbnail, download, an invite, and the activity dashboard, as both Owner and Member, against the `eu-west-1` backend), then again at the real `swordthain.com` post-cutover (fresh Owner sign-in, no CORS errors) to confirm the new origin works correctly — CORS is origin-specific, so passing on the CloudFront domain didn't guarantee `swordthain.com` would work too.

## Region split

Prompted by a real question worth recording: most of this project's users and downloads are UK-based, and downloads/streaming bypass CloudFront entirely (presigned S3 GET URLs go straight from browser to bucket — see the streaming section above), so the media bucket's region is exactly what download latency depends on. Everything CDK-managed had been built in `us-east-1` up to that point, mostly by default rather than by decision.

**What moved**: `apps/media-app`'s data plane — `MediaBucket`, the 4 DynamoDB tables, all 7 Lambda functions, `MediaHttpApi`, and `SiteBucket` — now in `SwordthainMediaAppDataStack`, `eu-west-1`.

**What stayed in `us-east-1`, and why**:
- `SwordthainAuthStack` (Cognito, OTP table, SES identity) — moving it would mean also moving playground's REST API + Lambda into CDK management (a much bigger, riskier change this project deliberately avoids), because **REST API Gateway's native `COGNITO_USER_POOLS` authorizer type requires the User Pool to be in the same region as the API** (confirmed against real AWS behavior — a genuine constraint, unlike HTTP API v2's JWT authorizer, which just validates against a JWKS URL over the network and has no such coupling). Playground's REST API and Lambda are both confirmed `us-east-1` (only its bucket is `eu-west-1`).
- `SwordthainPlaygroundStack` and the CloudFront/ACM/WAFv2 half of media-app hosting (`SwordthainMediaAppHostingStack`) — hard AWS constraints, not a choice. This doesn't hurt the data-residency goal: CloudFront is a global edge cache regardless of which region owns the distribution config, and no media bytes live there.

**Cross-region references are plain values, not CDK's `crossRegionReferences`** — consistent with this codebase's existing style of referencing fixed IDs directly (`hostedZoneId`, `playgroundApiId`) rather than importing full constructs across a boundary that isn't a same-region parent/child stack relationship. `MediaAppDataStack` takes Cognito's pool/client IDs and the SES identity ARN as plain strings computed in `bin/infra.ts`; `MediaAppHostingStack` and `CiStack` take `MediaAppDataStack`'s bucket name the same way.

**3 Lambda source files needed an explicit SDK client region** — everywhere a Lambda now living in `eu-west-1` calls a service that stayed in `us-east-1`: `infra/lambda/media/shares.ts` and `infra/lambda/media/activity.ts` (`CognitoIdentityProviderClient`), and `infra/lambda/media/invites.ts` (`CognitoIdentityProviderClient` *and* `SESv2Client`). Without it, the SDK client defaults to the Lambda's own runtime region and every `cognito-idp`/`ses` call fails.

**Two real bugs surfaced only by deploying, not by reading the CDK docs:**
- **`HttpUserPoolAuthorizer` builds its JWT issuer URL from the *deploying stack's* region, not the imported pool's actual region** — even when the pool is imported via `cognito.UserPool.fromUserPoolArn()` with a full, correctly-regioned ARN. First deploy attempt failed with a real `CREATE_FAILED`: `Invalid issuer: https://cognito-idp.eu-west-1.amazonaws.com/us-east-1_...` — API Gateway tried to fetch OIDC discovery from a URL that mismatches the pool ID's own region prefix. Fixed with the props override that exists for exactly this: `userPoolRegion: "us-east-1"`.
- **A transient S3 409 (`"A conflicting conditional operation is currently in progress against this resource"`) on recreating a bucket under the exact name an old, now-deleted `us-east-1` stack had used** — hit repeatedly across several deploy attempts and ~45 minutes despite the bucket confirmed absent (`head-bucket` 404) every time. Initially worked around by renaming the bucket, then reverted once a later attempt succeeded on the *original* name — the 409 turned out to be a genuine transient condition, not a permanent lock, just an unusually persistent one. If this recurs, waiting is the fix, not renaming.

**Deploy sequencing gotcha**: `MediaAppHostingStack` (us-east-1) references `MediaAppDataStack`'s (eu-west-1) bucket only by name, so it can — and during this migration, had to — be deployed *before* `MediaAppDataStack` exists, to break a circular dependency with `CiStack` (which needed `MediaAppHostingStack`'s distribution ID before the *old* `us-east-1` `SwordthainMediaAppStack` could be deleted, which in turn was blocking the bucket name `MediaAppDataStack` needed). CloudFront does not validate that an origin bucket exists at distribution-creation time, so this works — the distribution just won't serve real content until the bucket is actually there.

**DynamoDB tables with `RemovalPolicy.RETAIN` survive a failed deploy's automatic rollback** — every failed attempt during this migration that got far enough to create the 4 tables left them behind as real (if empty) resources after CloudFormation rolled back, colliding with the next retry. Each occurrence needed the same manual cleanup: confirm empty (`aws dynamodb scan --select COUNT`), delete, retry. Expected given `RETAIN`'s whole purpose is surviving stack deletion — just not an interaction that's obvious until you hit it.

### `SwordthainAuthStack`
The Cognito User Pool shared by both apps: friends sign in as `Member`, the owner as `Owner` (Cognito group, checked explicitly in app code — see the spec's section 9 on the playground's access control).

- **Passwordless email-OTP sign-in**, implemented as a Cognito custom authentication challenge (not Cognito's native "choice-based auth" — that mechanism requires password as an allowed factor, which doesn't fit a fully passwordless design):
  - `DefineAuthChallengeFn` — orchestrates the challenge, allows up to 3 attempts per sign-in session.
  - `CreateAuthChallengeFn` — generates a 6-digit code on the first attempt of a session, stores its hash in DynamoDB (`swordthain-otp-codes`, 5-minute TTL), and emails it via SES. One narrow special case: for exactly one pre-configured email (`REGRESSION_TEST_EMAIL`, `ci-test@swordthain.com`), it uses a fixed code read from SSM (`REGRESSION_TEST_OTP_PARAM`) instead of a random one, and skips the SES send — lets `infra/regression-tests` sign in with zero human interaction. Every other account's flow is unchanged, and `VerifyAuthChallengeResponseFn` needed no changes at all, since it just compares a submitted code's hash against whatever hash was stored, generically.
  - `VerifyAuthChallengeResponseFn` — checks the submitted code against the stored hash, single-use (deleted on success).
- **SES identity** for `swordthain.com` is provisioned and DNS-verified automatically (DKIM CNAMEs + MAIL FROM MX/TXT records added to the existing Route 53 hosted zone).
- **No password path**: the app client only allows `ALLOW_CUSTOM_AUTH` + `ALLOW_REFRESH_TOKEN_AUTH`. OAuth/Hosted UI is disabled — the app talks to Cognito directly.
- **Long-lived sessions**: refresh tokens valid 365 days (long-lived session per device, including smart TVs), access/ID tokens 1 hour.
- **MFA**: optional TOTP, on top of the inherent OTP-per-login factor.
- Owner/Member groups created; role checks should key off group membership, not a separate custom attribute.

**Not yet included — deliberately deferred:**
- Passkey/WebAuthn sign-in. Cognito's native passkey support lives in a different, newer auth-selection mechanism than the custom-Lambda OTP flow above, and forces `password` to remain an allowed factor — worth its own design pass rather than bolting on.
- Everything else in the spec (S3 media bucket, DynamoDB app tables, API Gateway, MediaConvert, WAF Bot Control, the two apps' own stacks) — later phases.

### `SwordthainPlaygroundStack`
New hosting for `apps/playground` at `labs.swordthain.com`, per the spec's section 9 (owner-only, "plain 404 unless already signed in"). Deliberately references playground's **existing** S3 bucket (`swordthain-demo-sites`) and Lambda (`swordthain-automation`) by name only — never imports them into CDK management, so this stack can't risk playground's currently-working manual deploy (`swordthain-playground-ci`, unaffected by anything here).

- **`LabsCertificate`** — ACM cert for `labs.swordthain.com`, DNS-validated automatically against the existing Route 53 hosted zone (same `HostedZone.fromHostedZoneAttributes` pattern `AuthStack` already uses for SES).
- **`LabsDistribution`** — a CloudFront distribution aliased to `labs.swordthain.com`, same S3 bucket as playground's existing distribution, fronted by the same CloudFront-scope WAFv2 pattern as `SwordthainMediaAppHostingStack`'s `SiteWebAcl`.
- **The bucket policy grant is a one-time manual step, not CDK-managed.** CDK's `addToResourcePolicy` is a documented no-op on an imported bucket (confirmed by a real synth-time warning: "Cannot update bucket policy of an imported bucket"), so after `LabsDistribution` exists, its Origin Access Control needs an explicit `s3:GetObject` grant merged into the bucket's existing policy — done via `aws s3api get-bucket-policy` / `put-bucket-policy`, adding a second statement scoped to the new distribution's ARN (`AllowCloudFrontServicePrincipalLabsDistribution`) alongside the original statement scoped to playground's old distribution (`E1AUXZ6C0Z7J9P`), which is left untouched. If `LabsDistribution` is ever destroyed and recreated, this step needs repeating for its new ID.
- **The bucket is in `eu-west-1`, not `us-east-1`** (this stack's region — forced by CloudFront/ACM/WAFv2-for-CloudFront all requiring `us-east-1`, unrelated to where the bucket actually lives). `s3.Bucket.fromBucketAttributes` needs an explicit `region: "eu-west-1"`; omitting it makes CDK assume same-region-as-stack and build the wrong `*.s3.us-east-1.amazonaws.com` regional endpoint — caught via a real `PermanentRedirect` from S3 when first deployed without it.
- **`StealthGateFn`** (`infra/cloudfront-functions/labs-stealth-gate.js`, CloudFront Functions JS 2.0, viewer-request) — the stealth-only 404 gate itself. Reads the `swordthain_session` cookie (set by `apps/media-app`'s `auth.ts` on `.swordthain.com`), decodes the ID token's payload, and returns a static 404 unless `exp` is in the future and `cognito:groups` contains `"Owner"`. Deliberately does **not** verify the JWT signature — CloudFront Functions have no crypto or network access, so this is only ever a stealth layer to keep the URL from looking "alive"; real authorization is the Cognito-authorizer retrofit on playground's API (see below), which does verify signatures.
  - **`cognito:groups` is a real JSON array here**, not the bracket-wrapped string (`"[Owner]"`) API Gateway's HTTP API JWT authorizer produces (see the media-app hardening bug above). Confirmed by decoding a real ID token from a live CLI-driven Cognito login before writing the check — this function decodes the raw ID token straight from the cookie, never passing through that authorizer's claims-flattening, so the two claim shapes are genuinely different depending on the code path, not a contradiction.
  - **A base64-decode instruction-limit bug, caught by testing against the real function, not just reading the code**: the first version hand-rolled a base64url decode (character-by-character, `indexOf` into a 64-char alphabet string). Against a real ~700-character ID token payload, this consistently failed in production with `RangeError: Instruction limit exceeded` — confirmed via `aws cloudfront test-function`, and reproduced even after optimizing to a precomputed lookup table (still ~50-60% of budget for just the decode loop). The runtime's built-in `atob` (confirmed available via `typeof atob !== "undefined"` in a real test-function call, alongside `Buffer` and `TextDecoder`) does the same decode at ~14% of budget. Lesson: CloudFront Functions' JS 2.0 runtime supports more real built-ins than its "restricted runtime" reputation suggests — reach for them before hand-rolling.
  - **A second, subtler bug in that same `atob` fix, only caught later, post-cutover, with a real Owner session cookie**: `atob()` throws `"Unexpected end of input"` on a base64url string that isn't padded back to a multiple of 4 — JWTs strip that padding per RFC 7515, and the fix never re-added it. This passed every test during initial development because the specific test tokens used happened to have payload lengths that didn't trigger it; it surfaced only when a differently-sized real token hit it after the DNS cutover, silently locking `labs.swordthain.com` for a real Owner despite the stealth gate logic itself being correct. Diagnosed by temporarily swapping in a version that echoes which check failed (`DECODE_ERROR:<message>`) via `aws cloudfront test-function` against the actual failing cookie value, rather than guessing. Fixed with `while (str.length % 4) str += "=";` before the `atob` call. Lesson: a passing test with one token proves the *logic path* works, not that *every* input on that path is handled — vary input shape/length, not just input validity, especially for anything doing manual encoding/decoding.
- **Route 53 A/ALIAS record** for `labs.swordthain.com` → `LabsDistribution`, added to the existing hosted zone (adding one record to an imported zone is safe and normal CDK usage — unlike the bucket, nothing about importing a *hosted zone* for new-record-only use risks existing records).
- **Live at the real domain.** This stack's own Route 53 record made `labs.swordthain.com` live from the start (unlike `swordthain.com`, which needed the alias freed from playground's old distribution first — see `SwordthainMediaAppHostingStack` above). Verified end-to-end both against the distribution's own `*.cloudfront.net` domain and at `labs.swordthain.com` directly: fetched logged-out (404), then with a real Owner ID token as the `swordthain_session` cookie (real playground content, 200).
- **A real cache-invalidation gap, only caught by exercising a real write through the UI, not just reading pages**: `LabsDistribution` uses `CachePolicy.CACHING_OPTIMIZED`, which ignores query strings entirely — so `assets/sites.json?v=<timestamp>`'s cache-busting param (already in `apps/playground/assets/app.js`, predating this stack) never actually varied the cache key. The app's real mechanism for freshness is Lambda-triggered invalidation after every write (`CLOUDFRONT_DISTRIBUTION_ID` env var on `swordthain-automation`, not CDK-managed), but that var still pointed at playground's old, now-alias-less distribution (`E1AUXZ6C0Z7J9P`) — so writes landed in S3 correctly but never invalidated the distribution actually serving `labs.swordthain.com`, silently serving stale content indefinitely. Caught by creating a real test company through the live UI and finding it missing from a fresh page load; confirmed via the network tab that the response body's own `"updated"` field was months stale despite S3 having the current data. Fixed with a one-time `aws lambda update-function-configuration` to point `CLOUDFRONT_DISTRIBUTION_ID` at `LabsDistribution`'s real ID instead — same "manual step, not CDK-managed" category as everything else touching playground's existing Lambda.

#### Playground API auth retrofit
Playground's REST API (`x7g9r0sdmc`) had **no authentication at all** on its 4 POST methods before this — genuinely open to anyone who found the URL. This closes that gap.

- **`PlaygroundAuthorizer`** — a `CfnAuthorizer` (type `COGNITO_USER_POOLS`, `identitySource: method.request.header.Authorization`) in `playground-stack.ts`, referencing the shared `AuthStack` user pool and the existing REST API by ID only (`restApiId: props.playgroundApiId`) — no `RestApi.fromRestApiId` L2 import, since a raw `CfnAuthorizer` only needs the ID string, keeping this consistent with "reference, don't adopt."
- **Attaching it to the 4 existing methods (`/create`, `/archive`, `/project/create`, `/project/delete`) and redeploying the `prod` stage is a one-time AWS CLI step, not CDK-managed** — same reasoning as the bucket policy grant above: CDK didn't create these methods, so it can't cleanly own mutating them without a full API import that risks the live deploy. Run once:
  ```bash
  for id in u6vwa6 zf4m7u 3j9bv6 ttx58f; do
    aws apigateway update-method --rest-api-id x7g9r0sdmc --resource-id "$id" --http-method POST \
      --patch-operations op=replace,path=/authorizationType,value=COGNITO_USER_POOLS \
                          op=replace,path=/authorizerId,value=<PlaygroundAuthorizerId output>
  done
  aws apigateway create-deployment --rest-api-id x7g9r0sdmc --stage-name prod
  ```
  Any *new* playground endpoint added later should specify `authorizerId`/`authorizationType` at method creation time instead of needing this script again.
- **`apps/playground/assets/app.js`** and **`apps/playground/company-template/index.html`** each got a small `authHeaders()`/`authHeader()` helper (duplicated rather than shared — they're genuinely separate script contexts: `app.js` serves the landing/archived pages, the template's inline script is copied fresh into every generated `/{company-id}/index.html` and never loads `app.js`) that reads the `swordthain_session` cookie and attaches it as `Authorization: Bearer <token>` on every existing `fetch()` call. No new login UI: by the time this JS runs on `labs.swordthain.com`, the CloudFront Function stealth gate has already required the cookie's presence just to serve the page, so signing in happens once, at `swordthain.com`.
  - **Only fixes future company pages, not already-generated ones** — `company-template/index.html` is copied by the Lambda at creation time into each company's own `/{company-id}/index.html`; editing the template doesn't retroactively patch pages already sitting in S3. Checked the live bucket before treating this as done: `sites.json` lists two company entries (`rossellimac`, `rossellimac-v2`), but neither has an actual generated page in S3 (confirmed via `aws s3api list-objects-v2` — only `company-template/`, `project-template/`, and the landing/archived pages exist), so there's nothing currently live to break. If real company pages exist by the time this is read, they'd need this same header fix applied directly (or regenerated) before their "Add project"/"Delete project" buttons work again.
- Verified against the real live API (not a synthetic test): all 4 endpoints return 401 with no `Authorization` header, and a real Owner ID token (minted via a full CLI-driven Cognito custom-auth flow) reaches the actual Lambda business logic — confirmed by getting the *app's own* validation errors (`"Company name is required"`, `"action must be 'archive', 'restore', or 'delete'"`) back instead of an API Gateway auth rejection, without needing to trigger a real create/archive/delete side effect against production data.

## Prerequisites

- Node.js 20+ (this repo was set up with Node 26 / npm 11).
- AWS credentials for account `584000479246` with permissions well beyond the read-only audit role — `cdk bootstrap` and `cdk deploy` create/modify CloudFormation stacks, IAM roles, Lambda functions, DynamoDB tables, Cognito resources, SES identities, and Route 53 records. A scoped policy or `AdministratorAccess`/`PowerUserAccess` + IAM role creation is realistically required.

## Commands

```bash
cd infra
npm install
npx tsc --noEmit      # type-check
npx cdk synth          # render CloudFormation locally, no AWS calls
npx cdk bootstrap aws://ACCOUNT/us-east-1   # one-time per account/region — creates CDK's asset bucket etc.
npx cdk bootstrap aws://ACCOUNT/eu-west-1   # also required since the region split — see "Region split" above
npx cdk diff            # compare against what's deployed
npx cdk deploy           # deploy SwordthainAuthStack
```

`cdk synth` has been run locally and produces a valid template (27 resources) — nothing has been deployed to AWS yet.
